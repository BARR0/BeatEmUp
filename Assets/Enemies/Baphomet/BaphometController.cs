using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaphometController : MonoBehaviour {
    public int life;
    public int xp;

	// Use this for initialization
	void Start () {
        Symbol halfHealth = new Symbol("halfHealth");
        //Symbol 
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
